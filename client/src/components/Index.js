import React from "react";

const Index = (props) => {
  const { numbers } = props;

  const mean = () => {};
  const median = () => {};
  const mode = () => {};

  return <div></div>;
};

export default Index;
